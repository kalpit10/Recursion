//Print 1 -> N (Cannot Use i + 1)

function count(i, n) {
  if (i < 1) return;

  count(i - 1, n);
  console.log(i);
}

count(3, 3);

// Print N -> 1 (Cannot use i - 1)

function count2(i, n) {
  if (i > n) return;

  count2(i + 1, n);
  console.log(i);
}
count2(1, 3);
